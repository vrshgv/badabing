services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: badabing
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: badabing
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: